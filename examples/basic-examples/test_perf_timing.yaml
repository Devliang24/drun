config:
  name: "性能计时"
  base_url: ${ENV(BASE_URL)}

steps:
  - name: 获取根路径耗时小于 2 秒
    request:
      method: GET
      path: /
    validate:
      - eq: [status_code, 200]
      - gt: [$elapsed_ms, 0]
      - lt: [$elapsed_ms, 2000]
