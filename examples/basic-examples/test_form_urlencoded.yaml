config:
  name: "表单 URL 编码（模板）"
  base_url: ${ENV(BASE_URL)}

steps:
  - name: 表单演示（跳过）
    skip: "template"
    request:
      method: POST
      url: /api/v1/some-form-endpoint
      headers:
        Content-Type: application/x-www-form-urlencoded
      data:
        a: 1
        b: two
