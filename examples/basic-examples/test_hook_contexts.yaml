config:
  name: "Hook 上下文演示"
  base_url: ${ENV(BASE_URL)}
  setup_hooks:
    - ${setup_hook_sign_request($request)}
  teardown_hooks:
    - ${teardown_hook_capture_request_id($response)}

steps:
  - name: 携带 Hooks 的根请求
    request:
      method: GET
      path: /
    validate:
      - eq: [status_code, 200]
