config:
  name: "多部分上传（模板）"
  base_url: ${ENV(BASE_URL)}

steps:
  - name: 上传演示（跳过）
    skip: "template"
    request:
      method: POST
      url: /api/v1/upload
      files:
        file1: "@path/to/file.bin"
      data:
        meta: "demo"
