config:
  name: "跳过与重试"
  base_url: ${ENV(BASE_URL)}

steps:
  - name: 跳过该步骤
    skip: "maintenance"
    request:
      method: GET
      url: /

  - name: 不稳定但重试
    retry: 2
    retry_backoff: 0.5
    request:
      method: GET
      url: /
    validate:
      - eq: [status_code, 200]
