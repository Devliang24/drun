config:
  name: "请求头合并"
  base_url: ${ENV(BASE_URL)}
  headers:
    Accept-Language: "zh-CN"

steps:
  - name: 步骤级覆盖请求头
    request:
      method: GET
      url: /
      headers:
        Accept-Language: "en-US"
    validate:
      - eq: [status_code, 200]
